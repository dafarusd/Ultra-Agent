.gradle/
build/
app/build/
local.properties
*.iml
.idea/
.cxx/
# Expo-era debug key — anyone holding it can sign over Build 29/30 installs.
# Backup lives in ~/keys/agent-ultra-debug.keystore.
app/debug.keystore
