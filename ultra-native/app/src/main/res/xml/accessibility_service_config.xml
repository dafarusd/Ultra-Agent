<?xml version="1.0" encoding="utf-8"?>
<!-- Not typeAllMask: Ultra reads the screen on demand, and a flood of events at 50 ms starved
     the other accessibility service on the machine (AndroidWorld's forwarder looped on "Could
     not get a11y tree"). These are the events it actually uses: which app is in front, and what
     a demonstration touched. -->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/accessibility_service_description"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged|typeViewClicked|typeViewTextChanged|typeViewScrolled|typeNotificationStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:notificationTimeout="200"
    android:canRetrieveWindowContent="true"
    android:canPerformGestures="true"
    android:canRequestFilterKeyEvents="true"
    android:settingsActivity="com.agent.ultra.MainActivity"
    android:accessibilityFlags="flagDefault|flagReportViewIds|flagIncludeNotImportantViews|flagRetrieveInteractiveWindows" />