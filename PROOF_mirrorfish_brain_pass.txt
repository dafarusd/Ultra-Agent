MIRRORFISH BRAIN PASS PROOF

==========================================================================
CMD 1: rg -n "event_trigger_remove" src/core/CommandParser.ts src/core/TaskExecutor.ts src/core/CapabilityRegistry.ts src/core/CapabilitySchemas.ts
==========================================================================
src/core/CommandParser.ts:768:    capability: 'event_trigger_remove',
src/core/TaskExecutor.ts:1681:      case 'event_trigger_remove': {
src/core/CapabilitySchemas.ts:451:    capabilityId: 'event_trigger_remove',
src/core/CapabilityRegistry.ts:80:      { id: 'event_trigger_remove', name: 'Remove Trigger', description: 'Remove an active event trigger by ID', riskLevel: 'moderate', available: true, permissionsRequired: [] },

==========================================================================
CMD 2: rg -n "allowLLMFallback" src/core/ReActLoop.ts src/core/AppIntelligence.ts src/core/TaskExecutor.ts
==========================================================================
src/core/ReActLoop.ts:26:  allowLLMFallback?: boolean;
src/core/ReActLoop.ts:58:  private allowLLMFallback: boolean;
src/core/ReActLoop.ts:66:    this.allowLLMFallback = options.allowLLMFallback ?? true;
src/core/ReActLoop.ts:82:    DebugLog.systemEvent('ReActLoop', `START goal="${goal.slice(0, 80)}" app="${appHint || 'any'}" llmFallback=${this.allowLLMFallback}`);
src/core/ReActLoop.ts:146:      if (!this.allowLLMFallback) {
src/core/ReActLoop.ts:300:    if (!this.allowLLMFallback) {
src/core/AppIntelligence.ts:152:      { maxIterations: Math.max(4, Math.min(8, maxSteps)), iterationDelayMs: 1100, allowLLMFallback: false },
src/core/TaskExecutor.ts:1560:          { maxIterations: hasAiFallback ? 8 : 10, iterationDelayMs: 1200, allowLLMFallback: hasAiFallback }

==========================================================================
CMD 3: rg -n "getEventMonitor" src/core/AgentCore.ts src/core/TaskExecutor.ts
==========================================================================
src/core/AgentCore.ts:1852:  getEventMonitor(): EventMonitor | null { return this.eventMonitor; }
src/core/TaskExecutor.ts:446:  private getEventMonitor(): import('../services/EventMonitor').EventMonitor | null {
src/core/TaskExecutor.ts:449:      return core?.getEventMonitor?.() ?? null;
src/core/TaskExecutor.ts:1641:        const monitor = this.getEventMonitor();
src/core/TaskExecutor.ts:1669:        const monitor = this.getEventMonitor();
src/core/TaskExecutor.ts:1682:        const monitor = this.getEventMonitor();

==========================================================================
CMD 4: rg -n "normalizeCommandInput|LEADING_POLITE_WRAPPERS|TRAILING_FILLER_PATTERNS" src/core/CommandParser.ts
==========================================================================
1082:const LEADING_POLITE_WRAPPERS: RegExp[] = [
1091:const TRAILING_FILLER_PATTERNS: RegExp[] = [
1096:function normalizeCommandInput(input: string): string {
1104:    for (const pattern of LEADING_POLITE_WRAPPERS) {
1112:  for (const pattern of TRAILING_FILLER_PATTERNS) {
1125:    let trimmed = normalizeCommandInput(input);

==========================================================================
CMD 5: rg -n "runGoal\(|EnhancedReActLoop|allowLLMFallback: false|extractFromScreen" src/core/AppIntelligence.ts
==========================================================================
3:import { EnhancedReActLoop } from './EnhancedReActLoop';
58:      const loopResult = await this.runGoal(goal, launched.packageName, options?.maxSteps ?? 12);
68:      const extraction = await this.extractFromScreen(query, reading, options?.extractPrompt);
105:      const loopResult = await this.runGoal(goal, launched.packageName, options?.maxSteps ?? 15);
107:      const extraction = await this.extractFromScreen(goal, reading, options?.extractPrompt);
135:    const extraction = await this.extractFromScreen(context || 'Describe what is on screen', reading);
149:  private async runGoal(goal: string, appPackage: string, maxSteps: number): Promise<GoalExecutionResult> {
152:      { maxIterations: Math.max(4, Math.min(8, maxSteps)), iterationDelayMs: 1100, allowLLMFallback: false },
160:    const hybridLoop = new EnhancedReActLoop(aiCall, aiCall, { maxIterations: maxSteps, iterationDelayMs: 1200 });
262:  private async extractFromScreen(query: string, reading: ScreenReading, extractPrompt?: string): Promise<{ summary: string; structuredData: any; confidence: number }> {

==========================================================================
CMD 6: rg -n "parseStoredTrigger|extractTriggerId|loadTriggers\(|removeTrigger\(|getTriggers\(" src/services/EventMonitor.ts
==========================================================================
33:    await this.loadTriggers();
211:  async removeTrigger(id: string): Promise<void> {
216:  getTriggers(): EventTrigger[] {
220:  private parseStoredTrigger(raw: string): Partial<EventTrigger> | null {
231:  private extractTriggerId(trigger: Partial<EventTrigger>, recordTrigger: string): string | null {
237:  private async loadTriggers(): Promise<void> {
247:      const parsed = this.parseStoredTrigger(record.outcome);
249:      const triggerId = this.extractTriggerId(parsed, record.trigger);

==========================================================================
CMD 7: git diff -- src/core/CommandParser.ts src/core/ReActLoop.ts src/core/AppIntelligence.ts src/core/TaskExecutor.ts src/core/AgentCore.ts src/services/EventMonitor.ts
==========================================================================
diff --git a/src/core/AgentCore.ts b/src/core/AgentCore.ts
index 8e187bc..dddd1f0 100644
--- a/src/core/AgentCore.ts
+++ b/src/core/AgentCore.ts
@@ -320,7 +320,9 @@ export class AgentCore extends SimpleEmitter {
       'sms_read', 'sms_conversation', 'sms_send', 'camera_capture', 'flashlight_toggle',
       'alarm_set', 'timer_set', 'reminder_create', 'clipboard_read', 'clipboard_write',
       'wifi_toggle', 'bluetooth_toggle', 'do_not_disturb', 'battery_status', 'system_info',
-      'device_info', 'app_share', 'web_research', 'vision_read'
+      'device_info', 'app_share', 'web_research', 'vision_read', 'react_navigate',
+      'event_trigger_set', 'event_trigger_list', 'event_trigger_remove', 'note_create',
+      'calendar_create', 'app_info', 'notification_read', 'volume_set', 'brightness_set'
     ].includes(plan.capability);
   }
 
@@ -1847,6 +1849,7 @@ You are always on. Always capable. Always direct.`;
   getCortex(): Cortex { return this.cortex; }
   getProactiveEngine(): ProactiveEngine | null { return this.proactive; }
   getBackgroundOrchestrator(): BackgroundOrchestrator | null { return this.background; }
+  getEventMonitor(): EventMonitor | null { return this.eventMonitor; }
   getKnowledgeGraph(): KnowledgeGraph { return this.cortex.getKnowledgeGraph(); }
   getDeviceSignals(): DeviceSignals { return this.deviceSignals; }
   getCostSummary() { return this.costTracker.getSummary(); }
diff --git a/src/core/AppIntelligence.ts b/src/core/AppIntelligence.ts
index ca70ed8..7d294bb 100644
--- a/src/core/AppIntelligence.ts
+++ b/src/core/AppIntelligence.ts
@@ -1,6 +1,7 @@
 import { Platform } from 'react-native';
 import AppController from '../native/AppController';
 import { EnhancedReActLoop } from './EnhancedReActLoop';
+import { ReActLoop } from './ReActLoop';
 import { lookupPackage, findBestMatch } from './AppDirectory';
 import type { ModelRouter } from './ModelRouter';
 import { UltraDevLog as DebugLog } from '../utils/UltraDevLog';
@@ -10,13 +11,29 @@ import { logAICall } from '../utils/AICallLogger';
 const isNative = Platform.OS !== 'web';
 
 export interface AppDataResult {
-  success: boolean; app: string; packageName: string; screenText: string[];
-  structuredData: any; aiSummary: string; confidence: number; steps: number; timestamp: number;
+  success: boolean;
+  app: string;
+  packageName: string;
+  screenText: string[];
+  structuredData: any;
+  aiSummary: string;
+  confidence: number;
+  steps: number;
+  timestamp: number;
 }
 
 interface ScreenReading {
   nodes: Array<{ i: number; t: string; d: string; c: boolean; e: boolean; s: boolean; x: number; y: number }>;
-  activePackage: string; allText: string[]; clickableLabels: string[]; editableCount: number;
+  activePackage: string;
+  allText: string[];
+  clickableLabels: string[];
+  editableCount: number;
+}
+
+interface GoalExecutionResult {
+  goalAchieved: boolean;
+  steps: number;
+  mode: 'deterministic' | 'hybrid';
 }
 
 export class AppIntelligence {
@@ -31,19 +48,47 @@ export class AppIntelligence {
       const serviceOn = await AppController.isServiceEnabled().catch(() => false);
       if (!serviceOn) return this.failResult(appName, 'Accessibility not enabled');
 
-      const launched = await this.launchApp(appName);
-      if (!launched.success) { const chromeLaunched = await this.launchApp('chrome'); if (!chromeLaunched.success) return this.failResult(appName, `Could not launch ${appName} or Chrome`); }
+      let launched = await this.launchApp(appName);
+      if (!launched.success) {
+        launched = await this.launchApp('chrome');
+        if (!launched.success) return this.failResult(appName, `Could not launch ${appName} or Chrome`);
+      }
 
       const goal = `Search for "${query}" and wait for results to load`;
-      const aiCall = async (prompt: string) => { const r = await this.ai.complete(prompt, { taskId: `appintel_${Date.now().toString(36)}`, agentId: 'app_intel', maxTokens: 600, temperature: 0.2 }); return r.content; };
-      const loop = new EnhancedReActLoop(aiCall, aiCall, { maxIterations: options?.maxSteps ?? 12, iterationDelayMs: 1200 });
-      const reactResult = await loop.execute(goal);
+      const loopResult = await this.runGoal(goal, launched.packageName, options?.maxSteps ?? 12);
       const reading = await this.readScreen();
-      DebugLog.push('APP_INTEL_SEARCH' as any, { event: 'screen_read', app: reading.activePackage, textItems: reading.allText.length, steps: reactResult.steps.length, goalAchieved: reactResult.goalAchieved });
+      DebugLog.push('APP_INTEL_SEARCH' as any, {
+        event: 'screen_read',
+        app: reading.activePackage,
+        textItems: reading.allText.length,
+        steps: loopResult.steps,
+        goalAchieved: loopResult.goalAchieved,
+        mode: loopResult.mode,
+      });
       const extraction = await this.extractFromScreen(query, reading, options?.extractPrompt);
-      DebugLog.push('APP_INTEL_SEARCH' as any, { event: 'done', query: query.slice(0, 40), confidence: extraction.confidence, steps: reactResult.steps.length });
-      return { success: extraction.confidence > 0.2, app: appName, packageName: reading.activePackage, screenText: reading.allText, structuredData: extraction.structuredData, aiSummary: extraction.summary, confidence: extraction.confidence, steps: reactResult.steps.length, timestamp: Date.now() };
-    } finally { disposeCorrId(); }
+      DebugLog.push('APP_INTEL_SEARCH' as any, {
+        event: 'done',
+        query: query.slice(0, 40),
+        confidence: extraction.confidence,
+        steps: loopResult.steps,
+        mode: loopResult.mode,
+      });
+
+      const confidence = loopResult.goalAchieved ? Math.max(extraction.confidence, 0.45) : extraction.confidence;
+      return {
+        success: loopResult.goalAchieved || confidence > 0.2,
+        app: appName,
+        packageName: reading.activePackage,
+        screenText: reading.allText,
+        structuredData: extraction.structuredData,
+        aiSummary: extraction.summary,
+        confidence,
+        steps: loopResult.steps,
+        timestamp: Date.now(),
+      };
+    } finally {
+      disposeCorrId();
+    }
   }
 
   async readApp(appName: string, goal: string, options?: { extractPrompt?: string; maxSteps?: number }): Promise<AppDataResult> {
@@ -53,38 +98,105 @@ export class AppIntelligence {
       if (!isNative || !AppController.isAvailable()) return this.failResult(appName, 'Requires Android with accessibility');
       const serviceOn = await AppController.isServiceEnabled().catch(() => false);
       if (!serviceOn) return this.failResult(appName, 'Accessibility not enabled');
+
       const launched = await this.launchApp(appName);
       if (!launched.success) return this.failResult(appName, `Could not launch ${appName}`);
-      const aiCall = async (prompt: string) => { const r = await this.ai.complete(prompt, { taskId: `appintel_${Date.now().toString(36)}`, agentId: 'app_intel', maxTokens: 600, temperature: 0.2 }); return r.content; };
-      const loop = new EnhancedReActLoop(aiCall, aiCall, { maxIterations: options?.maxSteps ?? 15, iterationDelayMs: 1200 });
-      const reactResult = await loop.execute(goal, launched.packageName);
+
+      const loopResult = await this.runGoal(goal, launched.packageName, options?.maxSteps ?? 15);
       const reading = await this.readScreen();
       const extraction = await this.extractFromScreen(goal, reading, options?.extractPrompt);
-      DebugLog.push('APP_INTEL_READAPP' as any, { event: 'done', app: appName, confidence: extraction.confidence, steps: reactResult.steps.length });
-      return { success: extraction.confidence > 0.2, app: appName, packageName: reading.activePackage, screenText: reading.allText, structuredData: extraction.structuredData, aiSummary: extraction.summary, confidence: extraction.confidence, steps: reactResult.steps.length, timestamp: Date.now() };
-    } finally { disposeCorrId(); }
+      DebugLog.push('APP_INTEL_READAPP' as any, {
+        event: 'done',
+        app: appName,
+        confidence: extraction.confidence,
+        steps: loopResult.steps,
+        mode: loopResult.mode,
+      });
+
+      const confidence = loopResult.goalAchieved ? Math.max(extraction.confidence, 0.45) : extraction.confidence;
+      return {
+        success: loopResult.goalAchieved || confidence > 0.2,
+        app: appName,
+        packageName: reading.activePackage,
+        screenText: reading.allText,
+        structuredData: extraction.structuredData,
+        aiSummary: extraction.summary,
+        confidence,
+        steps: loopResult.steps,
+        timestamp: Date.now(),
+      };
+    } finally {
+      disposeCorrId();
+    }
   }
 
   async readCurrentScreen(context?: string): Promise<AppDataResult> {
     const reading = await this.readScreen();
     const extraction = await this.extractFromScreen(context || 'Describe what is on screen', reading);
-    return { success: true, app: reading.activePackage, packageName: reading.activePackage, screenText: reading.allText, structuredData: extraction.structuredData, aiSummary: extraction.summary, confidence: extraction.confidence, steps: 0, timestamp: Date.now() };
+    return {
+      success: true,
+      app: reading.activePackage,
+      packageName: reading.activePackage,
+      screenText: reading.allText,
+      structuredData: extraction.structuredData,
+      aiSummary: extraction.summary,
+      confidence: extraction.confidence,
+      steps: 0,
+      timestamp: Date.now(),
+    };
+  }
+
+  private async runGoal(goal: string, appPackage: string, maxSteps: number): Promise<GoalExecutionResult> {
+    const deterministicLoop = new ReActLoop(
+      async () => 'ACTION: done',
+      { maxIterations: Math.max(4, Math.min(8, maxSteps)), iterationDelayMs: 1100, allowLLMFallback: false },
+    );
+    const deterministic = await deterministicLoop.execute(goal, appPackage);
+    if (deterministic.goalAchieved || !this.ai.hasApiKey()) {
+      return { goalAchieved: deterministic.goalAchieved, steps: deterministic.steps.length, mode: 'deterministic' };
+    }
+
+    const aiCall = this.createAiCall();
+    const hybridLoop = new EnhancedReActLoop(aiCall, aiCall, { maxIterations: maxSteps, iterationDelayMs: 1200 });
+    const hybrid = await hybridLoop.execute(goal, appPackage);
+    return { goalAchieved: hybrid.goalAchieved, steps: hybrid.steps.length, mode: 'hybrid' };
+  }
+
+  private createAiCall(): (prompt: string) => Promise<string> {
+    return async (prompt: string) => {
+      const response = await this.ai.complete(prompt, {
+        taskId: `appintel_${Date.now().toString(36)}`,
+        agentId: 'app_intel',
+        maxTokens: 600,
+        temperature: 0.2,
+      });
+      return response.content;
+    };
   }
 
   private async launchApp(appName: string): Promise<{ success: boolean; packageName: string }> {
     try {
       const AgentNative = (await import('../native/AgentNative')).default;
       let pkg = lookupPackage(appName);
-      if (!pkg) { const installed = await AgentNative.getInstalledApps(); const match = findBestMatch(appName.toLowerCase(), installed); if (match) pkg = match.packageName; }
+      if (!pkg) {
+        const installed = await AgentNative.getInstalledApps();
+        const match = findBestMatch(appName.toLowerCase(), installed);
+        if (match) pkg = match.packageName;
+      }
       if (!pkg) return { success: false, packageName: '' };
+
       const result = await AgentNative.launchApp(pkg);
       if (!result.success) return { success: false, packageName: pkg };
-      await new Promise(r => setTimeout(r, 2500));
+      await new Promise((resolve) => setTimeout(resolve, 2500));
       await AppController.allowPackage(pkg);
       await AppController.allowPackage('com.android.systemui');
-      try { const fg = await AppController.getActivePackage(); if (fg && fg !== pkg) await AppController.allowPackage(fg); } catch {}
+      try {
+        const fg = await AppController.getActivePackage();
+        if (fg && fg !== pkg) await AppController.allowPackage(fg);
+      } catch {
+        // ignore foreground allow failures
+      }
 
-      // Gap 16A: Auth wall detection after app launch
       try {
         const { AuthGate } = await import('./AuthGate');
         const authDetection = await AuthGate.detect();
@@ -101,15 +213,22 @@ export class AppIntelligence {
                 autoFill: (app: string, authType: any) => cv.autoFill(app, authType),
               };
             }
-          } catch {}
+          } catch {
+            // ignore vault lookup failures
+          }
           const authResult = await AuthGate.handle(authDetection, { userPresent, credentialVault });
           if (!authResult.success) return { success: false, packageName: pkg };
         }
-      } catch {}
+      } catch {
+        // ignore auth gate failures
+      }
 
       DebugLog.push('APP_INTEL_LAUNCH' as any, { event: 'launched', app: appName, packageName: pkg });
       return { success: true, packageName: pkg };
-    } catch (e: any) { DebugLog.error('AppIntelligence', `Launch ${appName} failed: ${e.message}`); return { success: false, packageName: '' }; }
+    } catch (e: any) {
+      DebugLog.error('AppIntelligence', `Launch ${appName} failed: ${e.message}`);
+      return { success: false, packageName: '' };
+    }
   }
 
   private async readScreen(): Promise<ScreenReading> {
@@ -118,38 +237,83 @@ export class AppIntelligence {
       result.activePackage = await AppController.getActivePackage();
       const flat = await AppController.getScreenContentFlat();
       const allNodes: typeof result.nodes = JSON.parse(flat);
-      // Gap 11: filter out noisy/invisible/empty nodes before analysis
       const SKIP_PATTERNS = /^(com\.|android\.|·|•|\.{3,}|\s*)$/;
-      result.nodes = allNodes.filter(n => {
-        const text = (n.t || n.d || '').trim();
+      result.nodes = allNodes.filter((node) => {
+        const text = (node.t || node.d || '').trim();
         if (!text) return false;
-        if (text.length < 2 && !n.c) return false; // skip single-char non-clickable
+        if (text.length < 2 && !node.c) return false;
         if (SKIP_PATTERNS.test(text)) return false;
         return true;
       });
-      for (const node of result.nodes) { const text = (node.t || node.d || '').trim(); if (text) { result.allText.push(text); if (node.c) result.clickableLabels.push(text); } if (node.e) result.editableCount++; }
-    } catch (e: any) { DebugLog.error('AppIntelligence', `Screen read failed: ${e.message}`); }
+      for (const node of result.nodes) {
+        const text = (node.t || node.d || '').trim();
+        if (text) {
+          result.allText.push(text);
+          if (node.c) result.clickableLabels.push(text);
+        }
+        if (node.e) result.editableCount++;
+      }
+    } catch (e: any) {
+      DebugLog.error('AppIntelligence', `Screen read failed: ${e.message}`);
+    }
     return result;
   }
 
   private async extractFromScreen(query: string, reading: ScreenReading, extractPrompt?: string): Promise<{ summary: string; structuredData: any; confidence: number }> {
-    if (!this.ai.hasApiKey()) return { summary: reading.allText.slice(0, 30).join('\n'), structuredData: null, confidence: 0.3 };
+    if (!this.ai.hasApiKey()) {
+      const text = reading.allText.slice(0, 40);
+      const summary = text.length > 0
+        ? text.slice(0, 10).join('\n')
+        : `Screen captured from ${reading.activePackage}`;
+      return { summary, structuredData: { lines: text }, confidence: text.length > 0 ? 0.35 : 0.15 };
+    }
+
     const screenContext = reading.allText.slice(0, 80).join('\n');
     const prompt = `You are reading an Android app screen.\n\nAPP: ${reading.activePackage}\nQUERY: "${query}"\n\nSCREEN CONTENT:\n${screenContext.slice(0, 6000)}\n\n${extractPrompt || 'Extract the most relevant information. Be specific with numbers, dates, prices.'}\n\nRespond:\nSUMMARY: Clear answer (2-5 sentences with real data)\nDATA: JSON of key extracted data\nCONFIDENCE: 0-1`;
+
     try {
       const aiLog = logAICall({ agentId: 'app_extractor', prompt, maxTokens: 800, temperature: 0.2 });
       const { withRetry } = await import('../utils/AICallLogger');
-      const aiResult = await withRetry(() => this.ai.complete(prompt, { taskId: `extract_${Date.now().toString(36)}`, agentId: 'app_extractor', maxTokens: 800, temperature: 0.2 }), { maxRetries: 2, agentId: 'app_extractor' });
+      const aiResult = await withRetry(
+        () => this.ai.complete(prompt, { taskId: `extract_${Date.now().toString(36)}`, agentId: 'app_extractor', maxTokens: 800, temperature: 0.2 }),
+        { maxRetries: 2, agentId: 'app_extractor' },
+      );
       aiLog.logResponse(aiResult.content, aiResult.cost);
       const content = aiResult.content;
       const sm = content.match(/SUMMARY:\s*(.+?)(?=DATA:|CONFIDENCE:|$)/is);
       const dm = content.match(/DATA:\s*(\{[\s\S]*?\})(?=\s*CONFIDENCE:|$)/i);
       const cm = content.match(/CONFIDENCE:\s*([\d.]+)/i);
-      let sd: any = null; if (dm) { try { sd = JSON.parse(dm[1]); } catch {} }
-      DebugLog.push('APP_INTEL_EXTRACT' as any, { event: 'extracted', app: reading.activePackage, confidence: cm ? parseFloat(cm[1]) : 0.5, hasData: !!sd });
-      return { summary: sm ? sm[1].trim() : content.slice(0, 500), structuredData: sd, confidence: cm ? Math.min(1, parseFloat(cm[1])) : 0.5 };
-    } catch (e: any) { return { summary: `Screen: ${reading.allText.length} elements from ${reading.activePackage}`, structuredData: null, confidence: 0.2 }; }
+      let sd: any = null;
+      if (dm) {
+        try {
+          sd = JSON.parse(dm[1]);
+        } catch {
+          sd = null;
+        }
+      }
+      const confidence = cm ? Math.min(1, parseFloat(cm[1])) : 0.5;
+      DebugLog.push('APP_INTEL_EXTRACT' as any, { event: 'extracted', app: reading.activePackage, confidence, hasData: !!sd });
+      return { summary: sm ? sm[1].trim() : content.slice(0, 500), structuredData: sd, confidence };
+    } catch {
+      return {
+        summary: `Screen: ${reading.allText.length} elements from ${reading.activePackage}`,
+        structuredData: { lines: reading.allText.slice(0, 30) },
+        confidence: 0.2,
+      };
+    }
   }
 
-  private failResult(app: string, reason: string): AppDataResult { return { success: false, app, packageName: '', screenText: [], structuredData: null, aiSummary: reason, confidence: 0, steps: 0, timestamp: Date.now() }; }
+  private failResult(app: string, reason: string): AppDataResult {
+    return {
+      success: false,
+      app,
+      packageName: '',
+      screenText: [],
+      structuredData: null,
+      aiSummary: reason,
+      confidence: 0,
+      steps: 0,
+      timestamp: Date.now(),
+    };
+  }
 }
diff --git a/src/core/CommandParser.ts b/src/core/CommandParser.ts
index 7193384..e5d7641 100644
--- a/src/core/CommandParser.ts
+++ b/src/core/CommandParser.ts
@@ -740,11 +740,34 @@ const rules: ParseRule[] = [
       action: m[2].trim(),
     }),
   },
+  {
+    pattern: /^(?:every|each)\s+(.+?)\s*,\s*(.+)$/i,
+    capability: 'event_trigger_set',
+    extractParams: (m) => ({
+      type: 'schedule',
+      condition: `every ${m[1].trim()}`,
+      action: m[2].trim(),
+    }),
+  },
+  {
+    pattern: /^at\s+(.+?)\s*,\s*(.+)$/i,
+    capability: 'event_trigger_set',
+    extractParams: (m) => ({
+      type: 'schedule',
+      condition: `at ${m[1].trim()}`,
+      action: m[2].trim(),
+    }),
+  },
   {
     pattern: /^(?:list|show)\s+(?:my\s+)?(?:triggers?|automations?|routines?)$/i,
     capability: 'event_trigger_list',
     extractParams: () => ({}),
   },
+  {
+    pattern: /^(?:remove|delete|cancel|disable|turn\s+off)\s+(?:the\s+)?(?:trigger|automation|routine)\s+(?:id\s+)?([a-z0-9:_\-]+)$/i,
+    capability: 'event_trigger_remove',
+    extractParams: (m) => ({ id: m[1].trim() }),
+  },
 
   // ════════════════════════════════════════════════════
   // MEMORY RECALL
@@ -1056,11 +1079,51 @@ const rules: ParseRule[] = [
   },
 ];
 
+const LEADING_POLITE_WRAPPERS: RegExp[] = [
+  /^(?:hey\s+)?ultra[\s,:-]+/i,
+  /^(?:please\s+)+/i,
+  /^(?:can|could|would|will)\s+you\s+/i,
+  /^(?:i\s+need\s+you\s+to|i\s+want\s+you\s+to|i\s+need\s+to|i\s+want\s+to)\s+/i,
+  /^(?:help\s+me\s+to|help\s+me)\s+/i,
+  /^(?:try\s+to|go\s+ahead\s+and)\s+/i,
+];
+
+const TRAILING_FILLER_PATTERNS: RegExp[] = [
+  /\s+(?:for\s+me|please|right\s+now|real\s+quick|really\s+quick)\s*$/i,
+  /[.!?]+$/,
+];
+
+function normalizeCommandInput(input: string): string {
+  let value = input.trim();
+  if (!value) return value;
+  value = value.replace(/[“”]/g, '"').replace(/[‘’]/g, "'");
+  value = value.replace(/^ultra[\s,]+/i, '');
+  let changed = true;
+  while (changed) {
+    changed = false;
+    for (const pattern of LEADING_POLITE_WRAPPERS) {
+      const next = value.replace(pattern, '');
+      if (next !== value) {
+        value = next.trim();
+        changed = true;
+      }
+    }
+  }
+  for (const pattern of TRAILING_FILLER_PATTERNS) {
+    value = value.replace(pattern, '').trim();
+  }
+  value = value.replace(/^please\s+/i, '').trim();
+  value = value.replace(/^open\s+up\s+/i, 'open ');
+  value = value.replace(/^look\s+for\s+/i, 'find ');
+  value = value.replace(/^look\s+up\s+/i, 'look up ');
+  value = value.replace(/\s+/g, ' ').trim();
+  return value;
+}
+
 export class CommandParser {
   parse(input: string): ActionPlan | null {
-    let trimmed = input.trim();
+    let trimmed = normalizeCommandInput(input);
     if (!trimmed) return null;
-    trimmed = trimmed.replace(/^ultra[\s,]+/i, '');
     // UltraDevLog imported lazily to avoid adding to top-level (circular risk with rules array)
     let _log: any = null;
     try { _log = require('../utils/UltraDevLog').UltraDevLog; } catch { _log = null; }
diff --git a/src/core/ReActLoop.ts b/src/core/ReActLoop.ts
index af24248..6bca0fc 100644
--- a/src/core/ReActLoop.ts
+++ b/src/core/ReActLoop.ts
@@ -23,11 +23,39 @@ export interface ReActResult {
 export interface ReActOptions {
   maxIterations?: number;
   iterationDelayMs?: number;
+  allowLLMFallback?: boolean;
 }
 
+interface FlatNode {
+  i: number;
+  t: string;
+  d: string;
+  c: boolean;
+  e: boolean;
+  s: boolean;
+  x: number;
+  y: number;
+}
+
+interface ParsedGoal {
+  action: 'search' | 'tap' | 'type' | 'scroll';
+  target: string;
+  value: string;
+}
+
+interface TextMatchOptions {
+  clickableOnly?: boolean;
+  preferNonEditable?: boolean;
+}
+
+const SEARCH_FIELD_HINT = /(search|find|query|lookup)/i;
+const SEARCH_BUTTON_HINT = /^(search|go|enter|submit|done|ok|apply)$/i;
+const RESULT_TEXT_HINT = /(result|results|price|rating|reviews?|buy|shop|watch|play|open|visit)/i;
+
 export class ReActLoop {
   private maxIterations: number;
   private iterationDelayMs: number;
+  private allowLLMFallback: boolean;
 
   constructor(
     private aiCall: (prompt: string) => Promise<string>,
@@ -35,6 +63,7 @@ export class ReActLoop {
   ) {
     this.maxIterations = options.maxIterations ?? 8;
     this.iterationDelayMs = options.iterationDelayMs ?? 1200;
+    this.allowLLMFallback = options.allowLLMFallback ?? true;
   }
 
   async execute(goal: string, appHint?: string): Promise<ReActResult> {
@@ -50,9 +79,8 @@ export class ReActLoop {
       return { success: false, steps, finalObservation: msg, goalAchieved: false, error: 'no_service' };
     }
 
-    DebugLog.systemEvent('ReActLoop', `START goal="${goal.slice(0, 80)}" app="${appHint || 'any'}"`);
+    DebugLog.systemEvent('ReActLoop', `START goal="${goal.slice(0, 80)}" app="${appHint || 'any'}" llmFallback=${this.allowLLMFallback}`);
 
-    // Wait for accessibility tree to populate — first read may be empty
     let observation = await this.observe();
     if (observation === 'Screen: empty or inaccessible' || observation === 'Screen: observation failed') {
       await this.sleep(1500);
@@ -62,25 +90,26 @@ export class ReActLoop {
         observation = await this.observe();
       }
     }
+
+    const parsedGoal = this.parseGoal(goal);
     let stuckCount = 0;
     let lastTreePrefix = '';
     let deterministicFailCount = 0;
 
     for (let iteration = 1; iteration <= this.maxIterations; iteration++) {
-      // TASK 2 SAFETY: verify we're NOT looking at our own UI each iteration
       try {
         const currentPkg = await AppController.getActivePackage();
         if (currentPkg === 'com.agent.ultra') {
           DebugLog.error('ReActLoop', `SAFETY STOP at iter ${iteration}: foreground package is Agent Ultra — aborting to prevent self-interaction`);
           return { success: false, steps, finalObservation: 'ReActLoop detected self-interaction — stopped for safety', goalAchieved: false, error: 'self_interaction' };
         }
-        // Re-allow current foreground package — handles app redirects, permission dialogs, mid-loop package changes
         if (currentPkg) await AppController.allowPackage(currentPkg);
-      } catch (e: any) { DebugLog.error('ReActLoop', `Safety check failed at iter ${iteration}: ${e?.message}`); }
+      } catch (e: any) {
+        DebugLog.error('ReActLoop', `Safety check failed at iter ${iteration}: ${e?.message}`);
+      }
 
-      // DETERMINISTIC: try to match goal to UI elements without LLM first
       const nodes = await this.getNodes();
-      const deterministicAction = nodes.length > 0 ? this.executeDeterministic(this.parseGoal(goal), nodes) : null;
+      const deterministicAction = nodes.length > 0 ? this.executeDeterministic(parsedGoal, nodes, steps) : null;
 
       if (deterministicAction) {
         DebugLog.systemEvent('ReActLoop', `STEP ${iteration}: DETERMINISTIC action="${deterministicAction}"`);
@@ -88,29 +117,40 @@ export class ReActLoop {
           steps.push({ iteration, observation, reasoning: 'deterministic:done', action: deterministicAction, actionResult: true, uiChanged: false });
           return { success: true, steps, finalObservation: observation, goalAchieved: true };
         }
+
         const beforeObs = observation;
         const actionResult = await this.executeAction(deterministicAction);
-        await this.sleep(this.iterationDelayMs);
+        await this.waitAfterAction();
         const newObservation = await this.observe();
         const uiChanged = newObservation !== beforeObs;
         steps.push({ iteration, observation, reasoning: 'deterministic', action: deterministicAction, actionResult, uiChanged });
         DebugLog.systemEvent('ReActLoop', `STEP ${iteration}: det="${deterministicAction}" result=${actionResult} uiChanged=${uiChanged}`);
-        const treePrefix2 = newObservation.slice(0, 80);
-        if (treePrefix2 === lastTreePrefix) {
+
+        const treePrefix = newObservation.slice(0, 80);
+        if (treePrefix === lastTreePrefix) {
           stuckCount++;
-          if (stuckCount >= 2) { await AppController.performScroll('down'); await this.sleep(600); stuckCount = 0; }
-        } else { stuckCount = 0; }
-        lastTreePrefix = treePrefix2;
+          if (stuckCount >= 2) {
+            await AppController.performScroll('down');
+            await this.sleep(600);
+            stuckCount = 0;
+          }
+        } else {
+          stuckCount = 0;
+        }
+        lastTreePrefix = treePrefix;
         observation = newObservation;
         continue;
-      } else {
-        deterministicFailCount++;
       }
 
-      // LLM FALLBACK: only when deterministic matching failed
+      deterministicFailCount++;
+      if (!this.allowLLMFallback) {
+        DebugLog.systemEvent('ReActLoop', `STEP ${iteration}: deterministic exhausted with no LLM fallback`);
+        break;
+      }
+
       DebugLog.systemEvent('ReActLoop', `STEP ${iteration}: Falling back to LLM (deterministic failed ${deterministicFailCount}x)`);
 
-      const systemPrompt = `You control an Android screen. You see UI elements and choose one action. Respond ONLY: ACTION: tap_index(N), tap(x,y), type("text"), scroll(up|down), back(), done. No explanation.`;
+      const systemPrompt = 'You control an Android screen. You see UI elements and choose one action. Respond ONLY: ACTION: tap_index(N), tap(x,y), type("text"), scroll(up|down), back(), done. No explanation.';
       const userMessage = `GOAL: ${goal}\n\nSCREEN:\n${observation.slice(0, 2000)}\n\nACTION:`;
       let reasoning: string;
       try {
@@ -134,7 +174,7 @@ export class ReActLoop {
 
       const beforeObservation = observation;
       const actionResult = await this.executeAction(action);
-      await this.sleep(this.iterationDelayMs);
+      await this.waitAfterAction();
       const newObservation = await this.observe();
       const uiChanged = newObservation !== beforeObservation;
 
@@ -156,7 +196,7 @@ export class ReActLoop {
       observation = newObservation;
     }
 
-    const goalAchieved = await this.checkCompletion(goal, observation);
+    const goalAchieved = await this.checkCompletion(parsedGoal, observation);
     DebugLog.systemEvent('ReActLoop', `COMPLETE goalAchieved=${goalAchieved} steps=${steps.length}`);
     return { success: goalAchieved, steps, finalObservation: observation, goalAchieved };
   }
@@ -164,13 +204,9 @@ export class ReActLoop {
   private async observe(): Promise<string> {
     try {
       const flat = await getScreenContentFlat();
-      const nodes = JSON.parse(flat) as Array<{
-        i: number; t: string; d: string;
-        c: boolean; e: boolean; s: boolean;
-        x: number; y: number;
-      }>;
-      if (nodes.length === 0) return 'Screen: empty or inaccessible';
-      return nodes.map(n => {
+      const nodes = JSON.parse(flat) as FlatNode[];
+      if (!Array.isArray(nodes) || nodes.length === 0) return 'Screen: empty or inaccessible';
+      return nodes.map((n) => {
         const label = (n.t || n.d || '').slice(0, 60);
         const flags: string[] = [];
         if (n.c) flags.push('tap');
@@ -181,51 +217,14 @@ export class ReActLoop {
     } catch {
       try {
         const tree = await AppController.getScreenContent();
-        return typeof tree === 'string' ? (tree as string).slice(0, 1500) : JSON.stringify(tree).slice(0, 1500);
+        const serialized = typeof (tree as any) === 'string' ? String(tree) : JSON.stringify(tree);
+        return serialized.slice(0, 1500);
       } catch {
         return 'Screen: observation failed';
       }
     }
   }
 
-  private buildPrompt(goal: string, observation: string, history: ReActStep[]): string {
-    const recentHistory = history.slice(-3).map(s =>
-      `Step ${s.iteration}: ${s.action} -> ${s.actionResult ? 'ok' : 'fail'}${s.uiChanged ? ' (screen changed)' : ' (no change)'}`
-    ).join('\n') || 'None';
-
-    return `You are controlling an Android phone. Analyze the screen and choose ONE action.
-
-GOAL: ${goal}
-
-CURRENT SCREEN (index, label, capabilities, coordinates):
-${observation.slice(0, 2500)}
-
-RECENT ACTIONS:
-${recentHistory}
-
-AVAILABLE ACTIONS:
-- tap(x,y) - tap screen coordinates
-- tap_index(N) - tap element by index [N]
-- type("text") - type into focused field
-- scroll(down) or scroll(up)
-- swipe(x1,y1,x2,y2)
-- back()
-- home()
-- done - goal is achieved
-
-RULES:
-1. Identify elements by label and index number.
-2. Choose the single most direct action toward the goal.
-3. If goal achieved: ACTION: done
-4. Never repeat an action that had no effect.
-5. If element not visible, scroll first.
-6. Prefer tap_index(N) over coordinates when possible.
-
-RESPOND WITH:
-REASONING: <brief analysis>
-ACTION: <single action command>`;
-  }
-
   private extractAction(text: string): string | null {
     const strict = text.match(/^ACTION:\s*(.+)$/im);
     if (strict) return strict[1].trim();
@@ -263,14 +262,14 @@ ACTION: <single action command>`;
       }
 
       const tapMatch = a.match(/^tap\(\s*(\d+)\s*,\s*(\d+)\s*\)$/i);
-      if (tapMatch) return await performTap(parseInt(tapMatch[1]), parseInt(tapMatch[2]));
+      if (tapMatch) return await performTap(parseInt(tapMatch[1], 10), parseInt(tapMatch[2], 10));
 
       const tapIdxMatch = a.match(/^tap_index\(\s*(\d+)\s*\)$/i);
       if (tapIdxMatch) {
-        const idx = parseInt(tapIdxMatch[1]);
+        const idx = parseInt(tapIdxMatch[1], 10);
         const flat = await getScreenContentFlat();
-        const nodes = JSON.parse(flat) as Array<{ i: number; x: number; y: number }>;
-        const node = nodes.find(n => n.i === idx);
+        const nodes = JSON.parse(flat) as FlatNode[];
+        const node = Array.isArray(nodes) ? nodes.find((n) => n.i === idx) : null;
         if (node) return await performTap(node.x, node.y);
         return false;
       }
@@ -281,9 +280,11 @@ ACTION: <single action command>`;
       const swipeMatch = a.match(/^swipe\(\s*(\d+)\s*,\s*(\d+)\s*,\s*(\d+)\s*,\s*(\d+)\s*\)$/i);
       if (swipeMatch) {
         return await performSwipe(
-          parseInt(swipeMatch[1]), parseInt(swipeMatch[2]),
-          parseInt(swipeMatch[3]), parseInt(swipeMatch[4]),
-          350
+          parseInt(swipeMatch[1], 10),
+          parseInt(swipeMatch[2], 10),
+          parseInt(swipeMatch[3], 10),
+          parseInt(swipeMatch[4], 10),
+          350,
         );
       }
 
@@ -295,97 +296,239 @@ ACTION: <single action command>`;
     }
   }
 
-  private async checkCompletion(goal: string, observation: string): Promise<boolean> {
+  private async checkCompletion(parsedGoal: ParsedGoal, observation: string): Promise<boolean> {
+    if (!this.allowLLMFallback) {
+      const nodes = await this.getNodes();
+      return this.heuristicCompletion(parsedGoal, nodes, observation);
+    }
+
     try {
-      const prompt = `Goal: "${goal}"\nCurrent screen:\n${observation.slice(0, 800)}\n\nIs the goal fully achieved? Reply YES or NO only.`;
+      const prompt = `Goal: "${parsedGoal.action} ${parsedGoal.target} ${parsedGoal.value}"\nCurrent screen:\n${observation.slice(0, 800)}\n\nIs the goal fully achieved? Reply YES or NO only.`;
       const response = await this.aiCall(prompt);
       return /^yes/i.test(response.trim());
     } catch {
-      return false;
+      const nodes = await this.getNodes();
+      return this.heuristicCompletion(parsedGoal, nodes, observation);
+    }
+  }
+
+  private heuristicCompletion(parsedGoal: ParsedGoal, nodes: FlatNode[], observation: string): boolean {
+    if (parsedGoal.action === 'search') {
+      const queryVisible = this.isQueryVisible(nodes, parsedGoal.value || parsedGoal.target);
+      const likelyResultsVisible = this.looksLikeSearchResults(nodes, parsedGoal.value || parsedGoal.target);
+      return queryVisible && (likelyResultsVisible || observation.toLowerCase().includes((parsedGoal.value || parsedGoal.target).toLowerCase()));
+    }
+    if (parsedGoal.action === 'tap') {
+      return !!this.findNodeByText(nodes, parsedGoal.target, { clickableOnly: false, preferNonEditable: false });
     }
+    return false;
+  }
+
+  private async waitAfterAction(): Promise<void> {
+    try {
+      await waitForUiChange(Math.min(1800, this.iterationDelayMs + 400));
+    } catch {
+      // ignore and rely on fixed delay
+    }
+    await this.sleep(Math.max(250, Math.min(900, this.iterationDelayMs)));
   }
 
   private sleep(ms: number): Promise<void> {
-    return new Promise(resolve => setTimeout(resolve, ms));
+    return new Promise((resolve) => setTimeout(resolve, ms));
   }
 
-  private async getNodes(): Promise<Array<{ i: number; t: string; d: string; c: boolean; e: boolean; s: boolean; x: number; y: number }>> {
+  private async getNodes(): Promise<FlatNode[]> {
     try {
       const flat = await getScreenContentFlat();
-      const parsed = JSON.parse(flat) as Array<{ i: number; t: string; d: string; c: boolean; e: boolean; s: boolean; x: number; y: number }>;
+      const parsed = JSON.parse(flat) as FlatNode[];
       return Array.isArray(parsed) ? parsed : [];
     } catch {
       return [];
     }
   }
 
-  private parseGoal(goal: string): { action: string; target: string; value: string } {
-    const g = goal.toLowerCase().trim();
-    const searchMatch = g.match(/^(?:search|find|look\s*up)\s+(?:for\s+)?["']?(.+?)["']?(?:\s+(?:in|on|using|within).*)?$/i);
-    if (searchMatch) return { action: 'search', target: 'search_field', value: searchMatch[1].trim() };
-    const tapMatch = g.match(/^(?:tap|click|press|select|choose)\s+(?:the\s+)?["']?(.+?)["']?(?:\s+button)?$/i);
-    if (tapMatch) return { action: 'tap', target: tapMatch[1].trim(), value: '' };
-    const typeMatch = g.match(/^(?:type|enter|input|fill\s+in)\s+["']?(.+?)["']?(?:\s+(?:in|into|to)\s+(.+))?$/i);
-    if (typeMatch) return { action: 'type', target: typeMatch[2]?.trim() || 'input_field', value: typeMatch[1].trim() };
-    const scrollMatch = g.match(/^scroll\s+(up|down)$/i);
-    if (scrollMatch) return { action: 'scroll', target: scrollMatch[1].toLowerCase(), value: '' };
+  private parseGoal(goal: string): ParsedGoal {
+    const raw = goal.trim();
+    const g = raw.toLowerCase();
+
+    const searchMatch = raw.match(/(?:search|find|look\s*up|browse|google)\s+(?:for\s+)?["']?(.+?)["']?(?:\s+(?:in|on|using|within).*)?$/i);
+    if (searchMatch) {
+      return { action: 'search', target: 'search_field', value: searchMatch[1].trim() };
+    }
+
+    const tapMatch = raw.match(/^(?:tap|click|press|select|choose)\s+(?:the\s+)?["']?(.+?)["']?(?:\s+button)?$/i);
+    if (tapMatch) {
+      return { action: 'tap', target: tapMatch[1].trim(), value: '' };
+    }
+
+    const typeMatch = raw.match(/^(?:type|enter|input|fill\s+in)\s+["']?(.+?)["']?(?:\s+(?:in|into|to)\s+(.+))?$/i);
+    if (typeMatch) {
+      return { action: 'type', target: typeMatch[2]?.trim() || 'input_field', value: typeMatch[1].trim() };
+    }
+
+    const scrollMatch = raw.match(/^scroll\s+(up|down)$/i);
+    if (scrollMatch) {
+      return { action: 'scroll', target: scrollMatch[1].toLowerCase(), value: '' };
+    }
+
     return { action: 'tap', target: g.slice(0, 40), value: '' };
   }
 
-  private findNodeByText(
-    nodes: Array<{ i: number; t: string; d: string; c: boolean; e: boolean; s: boolean; x: number; y: number }>,
-    text: string
-  ): { i: number; x: number; y: number } | null {
-    const needle = text.toLowerCase().trim();
-    const exact = nodes.find(n => (n.t || n.d || '').toLowerCase().trim() === needle && n.c);
-    if (exact) return exact;
-    const partial = nodes.find(n => (n.t || n.d || '').toLowerCase().includes(needle) && n.c);
-    if (partial) return partial;
-    const loose = nodes.find(n => needle.split(' ').some(word => word.length > 3 && (n.t || n.d || '').toLowerCase().includes(word)) && n.c);
-    return loose || null;
+  private normalizeText(value: string): string {
+    return value.toLowerCase().replace(/[^a-z0-9]+/g, ' ').replace(/\s+/g, ' ').trim();
+  }
+
+  private escapeForAction(value: string): string {
+    return value.replace(/\\/g, '\\\\').replace(/"/g, '\\"');
   }
 
-  private findEditableField(
-    nodes: Array<{ i: number; t: string; d: string; c: boolean; e: boolean; s: boolean; x: number; y: number }>
-  ): { i: number; x: number; y: number } | null {
-    const searchField = nodes.find(n => n.e && /search|query|find|q=/i.test(n.t + n.d));
-    if (searchField) return searchField;
-    const editableField = nodes.find(n => n.e);
-    return editableField || null;
+  private scoreTextMatch(label: string, target: string): number {
+    const normalizedLabel = this.normalizeText(label);
+    const normalizedTarget = this.normalizeText(target);
+    if (!normalizedLabel || !normalizedTarget) return 0;
+    if (normalizedLabel === normalizedTarget) return 100;
+    if (normalizedLabel.startsWith(normalizedTarget)) return 85;
+    if (normalizedLabel.includes(normalizedTarget)) return 75;
+
+    let score = 0;
+    const targetWords = normalizedTarget.split(' ').filter((word) => word.length > 2);
+    for (const word of targetWords) {
+      if (normalizedLabel === word) score += 30;
+      else if (normalizedLabel.startsWith(word)) score += 22;
+      else if (normalizedLabel.includes(word)) score += 16;
+    }
+    return score;
+  }
+
+  private findNodeByText(nodes: FlatNode[], text: string, options: TextMatchOptions = {}): FlatNode | null {
+    const matches = nodes
+      .filter((node) => !options.clickableOnly || node.c)
+      .map((node) => {
+        const label = `${node.t || ''} ${node.d || ''}`.trim();
+        let score = this.scoreTextMatch(label, text);
+        if (options.preferNonEditable && node.e) score -= 15;
+        if (!node.c && options.clickableOnly) score = 0;
+        return { node, score };
+      })
+      .filter((entry) => entry.score > 0)
+      .sort((a, b) => b.score - a.score || Number(a.node.e) - Number(b.node.e));
+
+    return matches.length > 0 ? matches[0].node : null;
   }
 
-  private findScrollable(
-    nodes: Array<{ i: number; t: string; d: string; c: boolean; e: boolean; s: boolean; x: number; y: number }>
-  ): { i: number; x: number; y: number } | null {
-    return nodes.find(n => n.s) || null;
+  private findSearchField(nodes: FlatNode[]): FlatNode | null {
+    const editable = nodes.filter((node) => node.e);
+    if (editable.length === 0) return null;
+
+    const scored = editable
+      .map((node) => {
+        const label = `${node.t || ''} ${node.d || ''}`.trim();
+        let score = SEARCH_FIELD_HINT.test(label) ? 100 : 10;
+        if (node.c) score += 5;
+        return { node, score };
+      })
+      .sort((a, b) => b.score - a.score);
+
+    return scored[0]?.node ?? null;
   }
 
-  private executeDeterministic(
-    parsed: { action: string; target: string; value: string },
-    nodes: Array<{ i: number; t: string; d: string; c: boolean; e: boolean; s: boolean; x: number; y: number }>
-  ): string | null {
+  private findSearchSubmit(nodes: FlatNode[]): FlatNode | null {
+    const candidates = nodes
+      .filter((node) => node.c && !node.e)
+      .map((node) => {
+        const label = `${node.t || ''} ${node.d || ''}`.trim();
+        let score = SEARCH_BUTTON_HINT.test(this.normalizeText(label)) ? 100 : 0;
+        if (!score && /search/i.test(label)) score = 85;
+        if (!score && /go|submit|done|apply|enter/i.test(label)) score = 75;
+        return { node, score };
+      })
+      .filter((entry) => entry.score > 0)
+      .sort((a, b) => b.score - a.score);
+
+    return candidates[0]?.node ?? null;
+  }
+
+  private isQueryVisible(nodes: FlatNode[], query: string): boolean {
+    const normalizedQuery = this.normalizeText(query);
+    if (!normalizedQuery) return false;
+    return nodes.some((node) => this.scoreTextMatch(`${node.t || ''} ${node.d || ''}`.trim(), normalizedQuery) >= 60);
+  }
+
+  private looksLikeSearchResults(nodes: FlatNode[], query: string): boolean {
+    const normalizedQuery = this.normalizeText(query);
+    let nonInputMatches = 0;
+    for (const node of nodes) {
+      if (node.e) continue;
+      const label = `${node.t || ''} ${node.d || ''}`.trim();
+      const normalizedLabel = this.normalizeText(label);
+      if (!normalizedLabel) continue;
+      if (normalizedLabel.includes(normalizedQuery) || RESULT_TEXT_HINT.test(label)) {
+        nonInputMatches++;
+      }
+      if (nonInputMatches >= 2) return true;
+    }
+    return false;
+  }
+
+  private executeDeterministic(parsed: ParsedGoal, nodes: FlatNode[], steps: ReActStep[]): string | null {
+    const lastAction = steps.length > 0 ? steps[steps.length - 1].action.trim().toLowerCase() : '';
+
     if (parsed.action === 'search') {
-      const field = this.findEditableField(nodes);
-      if (field) return `tap_index(${field.i})`;
-      const searchBtn = this.findNodeByText(nodes, 'search');
-      if (searchBtn) return `tap_index(${searchBtn.i})`;
+      const query = parsed.value || parsed.target;
+      const queryVisible = this.isQueryVisible(nodes, query);
+      const resultsVisible = this.looksLikeSearchResults(nodes, query);
+      const searchField = this.findSearchField(nodes);
+      const searchSubmit = this.findSearchSubmit(nodes);
+
+      if (!lastAction && searchField) {
+        return `tap_index(${searchField.i})`;
+      }
+
+      if (lastAction.startsWith('type(')) {
+        if (searchSubmit) return `tap_index(${searchSubmit.i})`;
+        if (queryVisible && resultsVisible) return 'done';
+        return null;
+      }
+
+      if (queryVisible && resultsVisible) return 'done';
+
+      if (lastAction.startsWith('tap_index(') || lastAction.startsWith('tap(')) {
+        if (searchField) return `type("${this.escapeForAction(query)}")`;
+      }
+
+      if (searchField && !queryVisible) {
+        return `tap_index(${searchField.i})`;
+      }
+
+      if (searchSubmit && queryVisible) {
+        return `tap_index(${searchSubmit.i})`;
+      }
+
+      if (queryVisible) return 'done';
       return null;
     }
+
     if (parsed.action === 'tap') {
-      const node = this.findNodeByText(nodes, parsed.target);
+      const node = this.findNodeByText(nodes, parsed.target, { clickableOnly: true, preferNonEditable: true });
       if (node) return `tap_index(${node.i})`;
       return null;
     }
+
     if (parsed.action === 'type') {
       const field = parsed.target !== 'input_field'
-        ? this.findNodeByText(nodes, parsed.target) || this.findEditableField(nodes)
-        : this.findEditableField(nodes);
-      if (field) return `type("${parsed.value}")`;
+        ? this.findNodeByText(nodes, parsed.target, { clickableOnly: true, preferNonEditable: false }) || this.findSearchField(nodes)
+        : this.findSearchField(nodes);
+      if (lastAction.startsWith('tap_index(') || lastAction.startsWith('tap(')) {
+        return `type("${this.escapeForAction(parsed.value)}")`;
+      }
+      if (field) return `tap_index(${field.i})`;
       return null;
     }
+
     if (parsed.action === 'scroll') {
       return `scroll(${parsed.target})`;
     }
+
     return null;
   }
-}
\ No newline at end of file
+}
diff --git a/src/core/TaskExecutor.ts b/src/core/TaskExecutor.ts
index 9d8902b..1015c47 100644
--- a/src/core/TaskExecutor.ts
+++ b/src/core/TaskExecutor.ts
@@ -443,6 +443,30 @@ export class TaskExecutor {
     }
   }
 
+  private getEventMonitor(): import('../services/EventMonitor').EventMonitor | null {
+    try {
+      const core = require('./AgentCore').getAgentCoreInstance?.();
+      return core?.getEventMonitor?.() ?? null;
+    } catch {
+      return null;
+    }
+  }
+
+  private inferTriggerType(condition: string): 'schedule' | 'battery_level' | 'notification' | 'sms_content' {
+    const normalized = condition.toLowerCase();
+    if (/battery|charge/.test(normalized)) return 'battery_level';
+    if (/notification|alert|from\s+app|app\s+opens?/.test(normalized)) return 'notification';
+    if (/sms|text\s+message|message\s+contains|texts?\s+from/.test(normalized)) return 'sms_content';
+    return 'schedule';
+  }
+
+  private summarizeTriggers(triggers: Array<{ id: string; type: string; condition: string; action: string; enabled: boolean }>): string {
+    if (triggers.length === 0) return 'No active triggers.';
+    return triggers
+      .map((trigger) => `• ${trigger.id} [${trigger.type}] ${trigger.enabled ? 'enabled' : 'disabled'} — when ${trigger.condition}, do ${trigger.action}`)
+      .join('\n');
+  }
+
   private async execWithParams(capId: string, params: Record<string, any>, request: string, taskId: string): Promise<any> {
     switch (capId) {
       case 'file_read': {
@@ -1484,7 +1508,7 @@ export class TaskExecutor {
           await AppController.openAccessibilitySettings();
           return { success: false, summary: 'Enable Agent Ultra in Accessibility Settings first' };
         }
-        // If appHint specifies an app, launch it first
+
         if (appHint) {
           const launchTarget = appHint.toLowerCase().trim();
           try {
@@ -1494,37 +1518,37 @@ export class TaskExecutor {
             if (match) {
               const launchResult = await AgentNativeModuleNav.launchApp(match.packageName);
               if (launchResult.success) {
-                // Wait for app to fully open
-                await new Promise(r => setTimeout(r, 2000));
-                // Allow the launched app for accessibility interaction
+                await new Promise((resolve) => setTimeout(resolve, 2000));
                 await AppController.allowPackage(match.packageName);
               }
             } else {
-              // Try launching via the static directory
-              const { lookupPackage } = await import('./AppDirectory');
               const knownPkg = lookupPackage(launchTarget);
               if (knownPkg) {
                 const launchResult = await AgentNativeModuleNav.launchApp(knownPkg);
                 if (launchResult.success) {
-                  await new Promise(r => setTimeout(r, 2000));
+                  await new Promise((resolve) => setTimeout(resolve, 2000));
                   await AppController.allowPackage(knownPkg);
                 }
               }
             }
           } catch (launchErr: any) {
             this.logger.warn(`react_navigate app launch failed: ${launchErr.message}`);
-            // Continue anyway — app might already be open
           }
         }
-        // Ensure whatever app is now in foreground is allowed for interaction
+
         try {
           const currentFg = await AppController.getActivePackage();
           if (currentFg) await AppController.allowPackage(currentFg);
-        } catch (e: any) { DebugLog.error('ReActNav', e?.message || 'foreground allow failed', e?.stack); }
+        } catch (e: any) {
+          DebugLog.error('ReActNav', e?.message || 'foreground allow failed', e?.stack);
+        }
         await AppController.allowPackage('com.android.systemui');
+
+        const hasAiFallback = this.ai.hasApiKey();
         const { ReActLoop } = await import('./ReActLoop');
         const reactLoop = new ReActLoop(
           async (prompt: string) => {
+            if (!hasAiFallback) return 'ACTION: done';
             const aiResult = await this.ai.complete(prompt, {
               taskId,
               agentId: 'react',
@@ -1533,10 +1557,10 @@ export class TaskExecutor {
             });
             return aiResult.content;
           },
-          { maxIterations: 8, iterationDelayMs: 1200 }
+          { maxIterations: hasAiFallback ? 8 : 10, iterationDelayMs: 1200, allowLLMFallback: hasAiFallback }
         );
         const reactResult = await reactLoop.execute(goal, appHint);
-        DebugLog.executorExit(taskId, 'react_navigate', reactResult.goalAchieved, `steps=${reactResult.steps.length}`);
+        DebugLog.executorExit(taskId, 'react_navigate', reactResult.goalAchieved, `steps=${reactResult.steps.length} llmFallback=${hasAiFallback}`);
         return {
           success: reactResult.goalAchieved,
           summary: reactResult.goalAchieved
@@ -1546,6 +1570,7 @@ export class TaskExecutor {
             steps: reactResult.steps.length,
             goalAchieved: reactResult.goalAchieved,
             finalObservation: reactResult.finalObservation.slice(0, 300),
+            llmFallbackUsed: hasAiFallback,
           },
         };
       }
@@ -1613,28 +1638,64 @@ export class TaskExecutor {
       }
 
       case 'event_trigger_set': {
-        const triggerId = `trigger_${Date.now()}`;
-        const triggerData = {
-          id: triggerId,
-          type: params.type || 'schedule',
-          condition: params.condition || '',
-          action: params.action || '',
+        const monitor = this.getEventMonitor();
+        if (!monitor) {
+          return { success: false, summary: 'Event monitor is not running yet.' };
+        }
+        const condition = typeof params.condition === 'string' ? params.condition.trim() : '';
+        const action = typeof params.action === 'string' ? params.action.trim() : '';
+        if (!condition || !action) {
+          return { success: false, summary: 'Trigger requires both a condition and an action.' };
+        }
+        const triggerType = typeof params.type === 'string' && params.type.trim()
+          ? params.type.trim()
+          : this.inferTriggerType(condition);
+        const triggerId = await monitor.addTrigger({
+          type: triggerType as any,
+          condition,
+          action,
           enabled: true,
-          createdAt: Date.now(),
-        };
-        DebugLog.systemEvent('event_trigger_set', `Trigger stored: ${JSON.stringify(triggerData).slice(0, 100)}`);
+        });
+        const triggers = monitor.getTriggers();
+        DebugLog.systemEvent('event_trigger_set', `Trigger stored: ${triggerId} type=${triggerType} condition="${condition.slice(0, 80)}"`);
         return {
           success: true,
-          summary: `Trigger set: when ${params.condition}, will ${params.action}. ID: ${triggerId}`,
-          data: triggerData,
+          summary: `Trigger set: when ${condition}, will ${action}. ID: ${triggerId}`,
+          data: { id: triggerId, type: triggerType, totalTriggers: triggers.length },
         };
       }
 
       case 'event_trigger_list': {
+        const monitor = this.getEventMonitor();
+        if (!monitor) {
+          return { success: false, summary: 'Event monitor is not running yet.' };
+        }
+        const triggers = monitor.getTriggers();
+        return {
+          success: true,
+          summary: this.summarizeTriggers(triggers),
+          data: { triggers, count: triggers.length },
+        };
+      }
+
+      case 'event_trigger_remove': {
+        const monitor = this.getEventMonitor();
+        if (!monitor) {
+          return { success: false, summary: 'Event monitor is not running yet.' };
+        }
+        const id = typeof params.id === 'string' ? params.id.trim() : '';
+        if (!id) {
+          return { success: false, summary: 'Which trigger ID should I remove?' };
+        }
+        const existing = monitor.getTriggers().find((trigger) => trigger.id === id);
+        if (!existing) {
+          return { success: false, summary: `No active trigger found with ID ${id}` };
+        }
+        await monitor.removeTrigger(id);
         return {
           success: true,
-          summary: 'Trigger listing available in debug log under EVENT_MONITOR_START entries.',
-          data: {},
+          summary: `Removed trigger ${id}`,
+          data: { id },
         };
       }
 
diff --git a/src/services/EventMonitor.ts b/src/services/EventMonitor.ts
index cb3e30c..94213b7 100644
--- a/src/services/EventMonitor.ts
+++ b/src/services/EventMonitor.ts
@@ -210,27 +210,64 @@ export class EventMonitor {
 
   async removeTrigger(id: string): Promise<void> {
     this.triggers = this.triggers.filter(t => t.id !== id);
-    await this.memory.storeLongterm(`trigger:${id}`, 'event_trigger', JSON.stringify({ deleted: true, id }));
+    await this.memory.storeLongterm(`trigger:${id}`, 'event_trigger', JSON.stringify({ deleted: true, id, timestamp: Date.now() }));
   }
 
   getTriggers(): EventTrigger[] {
-    return [...this.triggers];
+    return [...this.triggers].sort((a, b) => a.createdAt - b.createdAt);
+  }
+
+  private parseStoredTrigger(raw: string): Partial<EventTrigger> | null {
+    try {
+      const parsed = JSON.parse(raw) as Partial<EventTrigger> & { deleted?: boolean };
+      if (!parsed || typeof parsed !== 'object') return null;
+      return parsed;
+    } catch (e) {
+      DebugLog.error('EventMonitor', `Failed to parse trigger: ${e instanceof Error ? e.message : 'unknown error'}`);
+      return null;
+    }
+  }
+
+  private extractTriggerId(trigger: Partial<EventTrigger>, recordTrigger: string): string | null {
+    if (trigger.id && typeof trigger.id === 'string') return trigger.id;
+    const match = recordTrigger.match(/trigger:([A-Za-z0-9:_-]+)/);
+    return match ? match[1] : null;
   }
 
   private async loadTriggers(): Promise<void> {
     this.triggers = [];
-    const stored = await this.memory.retrieveRelevant('event_trigger', 50);
-    for (const record of stored) {
-      if (record.capability === 'event_trigger') {
-        try {
-          const trigger: EventTrigger = JSON.parse(record.outcome);
-          if (trigger.id && trigger.type && trigger.action && !(trigger as any).deleted) {
-            this.triggers.push(trigger);
-          }
-        } catch (e) {
-          DebugLog.error('EventMonitor', `Failed to parse trigger: ${e instanceof Error ? e.message : 'unknown error'}`);
-        }
+    const stored = await this.memory.retrieveRelevant('trigger', 200);
+    const byId = new Map<string, EventTrigger>();
+    const tombstones = new Set<string>();
+    const ordered = [...stored]
+      .filter(record => record.capability === 'event_trigger')
+      .sort((a, b) => a.timestamp - b.timestamp);
+
+    for (const record of ordered) {
+      const parsed = this.parseStoredTrigger(record.outcome);
+      if (!parsed) continue;
+      const triggerId = this.extractTriggerId(parsed, record.trigger);
+      if (!triggerId) continue;
+      if ((parsed as any).deleted) {
+        tombstones.add(triggerId);
+        byId.delete(triggerId);
+        continue;
       }
+      if (!parsed.type || !parsed.action) continue;
+      byId.set(triggerId, {
+        id: triggerId,
+        type: parsed.type as EventTrigger['type'],
+        condition: typeof parsed.condition === 'string' ? parsed.condition : '',
+        action: typeof parsed.action === 'string' ? parsed.action : '',
+        enabled: parsed.enabled !== false,
+        createdAt: typeof parsed.createdAt === 'number' ? parsed.createdAt : record.timestamp,
+        lastFired: typeof parsed.lastFired === 'number' ? parsed.lastFired : undefined,
+        metadata: parsed.metadata && typeof parsed.metadata === 'object' ? parsed.metadata : undefined,
+      });
     }
+
+    this.triggers = [...byId.values()]
+      .filter(trigger => !tombstones.has(trigger.id))
+      .sort((a, b) => a.createdAt - b.createdAt);
   }
 }
\ No newline at end of file

==========================================================================
CMD 8: npx tsc --noEmit (repo typecheck)
==========================================================================
app/index.tsx(326,30): error TS2345: Argument of type '"A11Y_STATE"' is not assignable to parameter of type 'UltraLogCat'.
app/index.tsx(371,28): error TS2345: Argument of type '"RENDER_PERF"' is not assignable to parameter of type 'UltraLogCat'.
app/index.tsx(404,24): error TS2345: Argument of type '"INIT_CHECKPOINT"' is not assignable to parameter of type 'UltraLogCat'.
app/index.tsx(415,24): error TS2345: Argument of type '"INIT_CHECKPOINT"' is not assignable to parameter of type 'UltraLogCat'.
app/index.tsx(435,43): error TS18047: 'vault' is possibly 'null'.
app/index.tsx(446,27): error TS18047: 'vault' is possibly 'null'.
app/index.tsx(451,19): error TS18047: 'vault' is possibly 'null'.
app/index.tsx(452,30): error TS2345: Argument of type '"VERSION_RESET"' is not assignable to parameter of type 'UltraLogCat'.
app/index.tsx(460,28): error TS2345: Argument of type '"VERSION_RESET"' is not assignable to parameter of type 'UltraLogCat'.
app/index.tsx(474,36): error TS18047: 'vault' is possibly 'null'.
app/index.tsx(478,21): error TS18047: 'vault' is possibly 'null'.
app/index.tsx(479,32): error TS2345: Argument of type '"STORAGE_MIGRATE"' is not assignable to parameter of type 'UltraLogCat'.
app/index.tsx(565,25): error TS2345: Argument of type 'SecureVault | null' is not assignable to parameter of type 'SecureVault'.
  Type 'null' is not assignable to type 'SecureVault'.
app/index.tsx(566,40): error TS18047: 'vault' is possibly 'null'.
app/index.tsx(581,26): error TS2345: Argument of type '"INIT_FATAL"' is not assignable to parameter of type 'UltraLogCat'.
app/index.tsx(707,17): error TS2322: Type '"system"' is not assignable to type '"command" | "conversation" | "ai_instruction" | undefined'.
app/index.tsx(750,21): error TS2322: Type '"vision"' is not assignable to type '"command" | "conversation" | "ai_instruction" | undefined'.
app/index.tsx(769,71): error TS2339: Property 'success' does not exist on type 'UltraExecutionResult'.
app/index.tsx(801,14): error TS2307: Cannot find module 'expo-notifications' or its corresponding type declarations.
app/index.tsx(802,74): error TS7006: Parameter 'response' implicitly has an 'any' type.
app/index.tsx(1026,27): error TS7006: Parameter 'm' implicitly has an 'any' type.
app/index.tsx(1365,155): error TS1117: An object literal cannot have multiple properties with the same name.
app/settings.tsx(177,13): error TS2322: Type '{ modelId: string; modelName: string; apiName: number; calls: number; inputTokens: number; outputTokens: number; cost: number; }[]' is not assignable to type 'ModelUsage[]'.
  Type '{ modelId: string; modelName: string; apiName: number; calls: number; inputTokens: number; outputTokens: number; cost: number; }' is not assignable to type 'ModelUsage'.
    Types of property 'apiName' are incompatible.
      Type 'number' is not assignable to type 'string'.
components/ActionGrid.tsx(57,28): error TS2345: Argument of type '"GRID_LOAD"' is not assignable to parameter of type 'UltraLogCat'.
components/ActionGrid.tsx(60,28): error TS2345: Argument of type '"GRID_LOAD"' is not assignable to parameter of type 'UltraLogCat'.
components/ActionGrid.tsx(63,26): error TS2345: Argument of type '"GRID_LOAD"' is not assignable to parameter of type 'UltraLogCat'.
components/ActionGrid.tsx(73,22): error TS2345: Argument of type '"GRID_SAVE"' is not assignable to parameter of type 'UltraLogCat'.
components/ActionGrid.tsx(176,22): error TS2345: Argument of type '"GRID_FAV_TOGGLE"' is not assignable to parameter of type 'UltraLogCat'.
components/ConversationList.tsx(234,24): error TS2345: Argument of type '"SIDEBAR_OPEN"' is not assignable to parameter of type 'UltraLogCat'.
components/ConversationList.tsx(273,22): error TS2345: Argument of type '"SIDEBAR_CONV_SELECT"' is not assignable to parameter of type 'UltraLogCat'.
components/ConversationList.tsx(372,38): error TS2345: Argument of type '"TOGGLE_TAP"' is not assignable to parameter of type 'UltraLogCat'.
components/ConversationList.tsx(397,51): error TS2345: Argument of type '"SIDEBAR_QUICK"' is not assignable to parameter of type 'UltraLogCat'.
src/core/AgentCore.ts(177,49): error TS2554: Expected 2 arguments, but got 3.
src/core/AgentCore.ts(179,50): error TS2554: Expected 2 arguments, but got 4.
src/core/AgentCore.ts(1165,36): error TS2339: Property 'requiresFuzzyConfirmation' does not exist on type '{ promptTrace?: PromptTrace | undefined; mode?: "command" | "conversation" | "ai_instruction" | undefined; capability?: string | undefined; risk?: "safe" | "moderate" | "dangerous" | "blocked" | undefined; ... 11 more ...; originalMessage?: string | undefined; }'.
src/core/AgentCore.ts(1166,100): error TS2339: Property 'candidates' does not exist on type '{ promptTrace?: PromptTrace | undefined; mode?: "command" | "conversation" | "ai_instruction" | undefined; capability?: string | undefined; risk?: "safe" | "moderate" | "dangerous" | "blocked" | undefined; ... 11 more ...; originalMessage?: string | undefined; }'.
src/core/AgentCore.ts(1167,54): error TS2339: Property 'fuzzyQuery' does not exist on type '{ promptTrace?: PromptTrace | undefined; mode?: "command" | "conversation" | "ai_instruction" | undefined; capability?: string | undefined; risk?: "safe" | "moderate" | "dangerous" | "blocked" | undefined; ... 11 more ...; originalMessage?: string | undefined; }'.
src/core/AgentCore.ts(1518,63): error TS2353: Object literal may only specify known properties, and 'requiresApproval' does not exist in type '{ promptTrace?: PromptTrace | undefined; mode?: "command" | "conversation" | "ai_instruction" | undefined; capability?: string | undefined; risk?: "safe" | "moderate" | "dangerous" | "blocked" | undefined; ... 11 more ...; originalMessage?: string | undefined; }'.
src/core/AgentCore.ts(1774,73): error TS2339: Property 'inputPer1k' does not exist on type 'PricingInfo'.
src/core/AgentCore.ts(1775,75): error TS2339: Property 'outputPer1k' does not exist on type 'PricingInfo'.
src/core/BackgroundOrchestrator.ts(29,42): error TS2307: Cannot find module 'expo-notifications' or its corresponding type declarations.
src/core/BackgroundOrchestrator.ts(53,46): error TS2307: Cannot find module 'expo-notifications' or its corresponding type declarations.
src/core/BuildSystem.ts(122,45): error TS2345: Argument of type 'number' is not assignable to parameter of type 'string'.
src/core/BuildSystem.ts(125,70): error TS2554: Expected 2-3 arguments, but got 4.
src/core/Cortex.ts(181,24): error TS2663: Cannot find name 'taskTotalCost'. Did you mean the instance member 'this.taskTotalCost'?
src/core/DeviceContext.ts(233,89): error TS2322: Type 'number | null' is not assignable to type 'number'.
  Type 'null' is not assignable to type 'number'.
src/core/IntentResolver.ts(342,30): error TS2339: Property 'VIEW' does not exist on type 'typeof ActivityAction'.
src/core/ModelRouter.ts(271,53): error TS2554: Expected 2 arguments, but got 3.
src/core/ModelRouter.ts(283,49): error TS2554: Expected 2 arguments, but got 3.
src/core/ModelRouter.ts(319,54): error TS2554: Expected 2 arguments, but got 3.
src/core/ModelRouter.ts(333,56): error TS2554: Expected 2 arguments, but got 3.
src/core/ModelRouter.ts(402,34): error TS2554: Expected 0 arguments, but got 1.
src/core/ModelRouter.ts(426,21): error TS2345: Argument of type '"NET_MODELS_RAW"' is not assignable to parameter of type 'UltraLogCat'.
src/core/ModelRouter.ts(483,21): error TS2345: Argument of type '"MODEL_DISCOVERY_DETAIL"' is not assignable to parameter of type 'UltraLogCat'.
src/core/ModelRouter.ts(493,50): error TS2554: Expected 1 arguments, but got 2.
src/core/ModelRouter.ts(717,56): error TS2554: Expected 2 arguments, but got 3.
src/core/ModelRouter.ts(789,109): error TS2554: Expected 4-5 arguments, but got 6.
src/core/ModelRouter.ts(887,100): error TS2554: Expected 4-5 arguments, but got 6.
src/core/ModelRouter.ts(909,16): error TS2554: Expected 2 arguments, but got 1.
src/core/ModelRouter.ts(950,50): error TS2554: Expected 2 arguments, but got 3.
src/core/ModelRouter.ts(1029,100): error TS2554: Expected 4-5 arguments, but got 6.
src/core/ModelRouter.ts(1084,39): error TS2345: Argument of type 'number' is not assignable to parameter of type 'string'.
src/core/ModelRouter.ts(1117,63): error TS2554: Expected 3 arguments, but got 4.
src/core/SettingsDirectory.ts(53,28): error TS2551: Property 'DEVICE_ADMIN_SETTINGS' does not exist on type 'typeof ActivityAction'. Did you mean 'DEVICE_INFO_SETTINGS'?
src/core/SettingsDirectory.ts(59,28): error TS2339: Property 'PERMISSION_USAGE_SETTINGS' does not exist on type 'typeof ActivityAction'.
src/core/TaskExecutor.ts(731,33): error TS2345: Argument of type '"CAMERA_RECOVERY"' is not assignable to parameter of type 'UltraLogCat'.
src/core/TaskExecutor.ts(1032,45): error TS2339: Property 'VIEW' does not exist on type 'typeof ActivityAction'.
src/core/TaskExecutor.ts(1227,54): error TS2339: Property 'suggest' does not exist on type 'typeof AppFallback'.
src/core/TaskExecutor.ts(1241,103): error TS2345: Argument of type '"native_launch_verify"' is not assignable to parameter of type '"device_query" | "ai_fallback" | "no_package" | "intent_launch"'.
src/core/TaskExecutor.ts(1245,56): error TS2339: Property 'suggest' does not exist on type 'typeof AppFallback'.
src/core/TaskExecutor.ts(1387,30): error TS2339: Property 'getBestGeneration' does not exist on type 'GenomeLineage'.
src/core/TaskExecutor.ts(1388,34): error TS2339: Property 'getFailurePatterns' does not exist on type 'GenomeLineage'.
src/core/TaskExecutor.ts(1400,47): error TS7006: Parameter 'f' implicitly has an 'any' type.
src/core/TaskExecutor.ts(1400,77): error TS7006: Parameter 'f' implicitly has an 'any' type.
src/core/TaskExecutor.ts(2340,44): error TS2339: Property 'exec' does not exist on type 'AppControllerInterface'.
src/core/VisionPipeline.ts(46,47): error TS18047: 'screenshotBase64' is possibly 'null'.
src/core/provider/AiService.ts(380,22): error TS2345: Argument of type '"AI_REQUEST"' is not assignable to parameter of type 'UltraLogCat'.
src/core/provider/AiService.ts(425,22): error TS2345: Argument of type '"AI_REQUEST"' is not assignable to parameter of type 'UltraLogCat'.
src/core/provider/AiService.ts(458,22): error TS2345: Argument of type '"AI_REQUEST"' is not assignable to parameter of type 'UltraLogCat'.
src/core/provider/AiService.ts(492,22): error TS2345: Argument of type '"AI_REQUEST"' is not assignable to parameter of type 'UltraLogCat'.
src/core/provider/GroupRouter.ts(64,28): error TS2345: Argument of type '"ROUTE"' is not assignable to parameter of type 'UltraLogCat'.
src/core/provider/GroupRouter.ts(77,32): error TS2345: Argument of type '"ROUTE"' is not assignable to parameter of type 'UltraLogCat'.
src/core/provider/GroupRouter.ts(92,32): error TS2345: Argument of type '"ROUTE"' is not assignable to parameter of type 'UltraLogCat'.
src/core/provider/GroupRouter.ts(105,26): error TS2345: Argument of type '"ROUTE"' is not assignable to parameter of type 'UltraLogCat'.
src/core/provider/GroupRouter.ts(107,26): error TS2345: Argument of type '"ROUTE"' is not assignable to parameter of type 'UltraLogCat'.
src/core/provider/GroupRouter.ts(111,28): error TS2345: Argument of type '"ROUTE"' is not assignable to parameter of type 'UltraLogCat'.
src/core/provider/GroupRouter.ts(115,30): error TS2345: Argument of type '"ROUTE"' is not assignable to parameter of type 'UltraLogCat'.
src/core/provider/GroupRouter.ts(121,24): error TS2345: Argument of type '"ROUTE"' is not assignable to parameter of type 'UltraLogCat'.
src/core/provider/GroupRouter.ts(128,26): error TS2345: Argument of type '"ROUTE"' is not assignable to parameter of type 'UltraLogCat'.
src/core/provider/GroupRouter.ts(131,28): error TS2345: Argument of type '"ROUTE"' is not assignable to parameter of type 'UltraLogCat'.
src/core/provider/GroupRouter.ts(143,28): error TS2345: Argument of type '"ROUTE"' is not assignable to parameter of type 'UltraLogCat'.
src/core/provider/GroupRouter.ts(155,28): error TS2345: Argument of type '"ROUTE"' is not assignable to parameter of type 'UltraLogCat'.
src/core/provider/GroupRouter.ts(162,22): error TS2345: Argument of type '"ROUTE"' is not assignable to parameter of type 'UltraLogCat'.
src/core/provider/GroupRouter.ts(276,26): error TS2345: Argument of type '"ROUTE"' is not assignable to parameter of type 'UltraLogCat'.
src/core/provider/GroupRouter.ts(280,26): error TS2345: Argument of type '"ROUTE"' is not assignable to parameter of type 'UltraLogCat'.
src/core/provider/GroupRouter.ts(285,26): error TS2345: Argument of type '"ROUTE"' is not assignable to parameter of type 'UltraLogCat'.
src/core/provider/GroupRouter.ts(289,26): error TS2345: Argument of type '"ROUTE"' is not assignable to parameter of type 'UltraLogCat'.
src/core/provider/GroupRouter.ts(293,26): error TS2345: Argument of type '"ROUTE"' is not assignable to parameter of type 'UltraLogCat'.
src/core/provider/GroupRouter.ts(303,26): error TS2345: Argument of type '"ROUTE"' is not assignable to parameter of type 'UltraLogCat'.
src/core/provider/GroupRouter.ts(356,24): error TS2345: Argument of type '"ROUTE"' is not assignable to parameter of type 'UltraLogCat'.
src/core/provider/GroupRouter.ts(361,24): error TS2345: Argument of type '"ROUTE"' is not assignable to parameter of type 'UltraLogCat'.
src/core/provider/GroupRouter.ts(371,24): error TS2345: Argument of type '"ROUTE"' is not assignable to parameter of type 'UltraLogCat'.
src/core/provider/GroupRouter.ts(380,24): error TS2345: Argument of type '"ROUTE"' is not assignable to parameter of type 'UltraLogCat'.
src/core/provider/GroupRouter.ts(385,24): error TS2345: Argument of type '"ROUTE"' is not assignable to parameter of type 'UltraLogCat'.
src/core/provider/GroupRouter.ts(402,7): error TS2322: Type 'string | undefined' is not assignable to type 'string'.
  Type 'undefined' is not assignable to type 'string'.
src/core/provider/GroupRouter.ts(403,7): error TS2322: Type 'string | undefined' is not assignable to type 'string'.
  Type 'undefined' is not assignable to type 'string'.
src/core/provider/RouteHistoryStore.ts(45,7): error TS2322: Type 'string | undefined' is not assignable to type 'string'.
  Type 'undefined' is not assignable to type 'string'.
src/genome/GenomeFitness.ts(69,7): error TS2353: Object literal may only specify known properties, and 'avgExecutionTimeMs' does not exist in type 'TaskPerformance'.
src/genome/GenomeFitness.ts(97,55): error TS2339: Property 'enabled' does not exist on type 'Capability'.
src/genome/GenomeMutator.ts(100,32): error TS2353: Object literal may only specify known properties, and 'error' does not exist in type 'MutationResult'.
src/genome/GenomeMutator.ts(113,32): error TS2353: Object literal may only specify known properties, and 'error' does not exist in type 'MutationResult'.
src/genome/GenomeMutator.ts(117,29): error TS2353: Object literal may only specify known properties, and 'mutatedGenome' does not exist in type 'MutationResult'.
src/genome/GenomeMutator.ts(126,7): error TS2353: Object literal may only specify known properties, and 'payload' does not exist in type 'MutationRecord'.
src/genome/GenomeMutator.ts(155,21): error TS2339: Property 'config' does not exist on type 'Genome'.
src/genome/GenomeMutator.ts(155,36): error TS2339: Property 'config' does not exist on type 'Genome'.
src/genome/GenomeMutator.ts(156,30): error TS2339: Property 'config' does not exist on type 'Genome'.
src/genome/GenomeMutator.ts(168,21): error TS2339: Property 'permissions' does not exist on type 'Genome'.
src/genome/GenomeMutator.ts(168,41): error TS2339: Property 'permissions' does not exist on type 'Genome'.
src/genome/GenomeMutator.ts(169,21): error TS2339: Property 'permissions' does not exist on type 'Genome'.
src/genome/GenomeMutator.ts(170,18): error TS2339: Property 'permissions' does not exist on type 'Genome'.
src/genome/GenomeMutator.ts(175,20): error TS2339: Property 'permissions' does not exist on type 'Genome'.
src/genome/GenomeMutator.ts(176,18): error TS2339: Property 'permissions' does not exist on type 'Genome'.
src/genome/GenomeMutator.ts(176,39): error TS2339: Property 'permissions' does not exist on type 'Genome'.
src/genome/GenomeMutator.ts(176,58): error TS7006: Parameter 'p' implicitly has an 'any' type.
src/genome/GenomeValidator.ts(70,19): error TS2339: Property 'mutatedGenome' does not exist on type 'MutationResult'.
src/genome/GenomeValidator.ts(71,51): error TS2339: Property 'mutatedGenome' does not exist on type 'MutationResult'.
src/genome/GenomeValidator.ts(92,61): error TS2339: Property 'riskLevel' does not exist on type 'Capability'.
src/genome/GenomeValidator.ts(99,30): error TS2339: Property 'enabled' does not exist on type '{ invariants: SafetyInvariant[]; approvalRequired: string[]; autoApproved: string[]; blocked: string[]; maxShellTimeoutMs: number; shellAllowlist: string[]; maxBuildRetries: number; maxMutationsPerCycle: number; }'.
src/genome/SelfImprover.ts(102,41): error TS2339: Property 'applyAll' does not exist on type 'GenomeMutator'.
src/genome/SelfImprover.ts(207,39): error TS2339: Property 'compareGenerations' does not exist on type 'GenomeFitness'.
src/genome/SelfImprover.ts(217,74): error TS7006: Parameter 'm' implicitly has an 'any' type.
src/services/CostTracker.ts(111,25): error TS2339: Property 'inputPer1kTokens' does not exist on type '{ input: number; output: number; } | { inputPer1kTokens?: number | undefined; outputPer1kTokens?: number | undefined; }'.
  Property 'inputPer1kTokens' does not exist on type '{ input: number; output: number; }'.
src/services/CostTracker.ts(111,65): error TS2339: Property 'outputPer1kTokens' does not exist on type '{ input: number; output: number; } | { inputPer1kTokens?: number | undefined; outputPer1kTokens?: number | undefined; }'.
  Property 'outputPer1kTokens' does not exist on type '{ input: number; output: number; }'.
src/services/CostTracker.ts(112,45): error TS2339: Property 'inputPer1kTokens' does not exist on type '{ input: number; output: number; } | { inputPer1kTokens?: number | undefined; outputPer1kTokens?: number | undefined; }'.
  Property 'inputPer1kTokens' does not exist on type '{ input: number; output: number; }'.
src/services/CostTracker.ts(113,46): error TS2339: Property 'outputPer1kTokens' does not exist on type '{ input: number; output: number; } | { inputPer1kTokens?: number | undefined; outputPer1kTokens?: number | undefined; }'.
  Property 'outputPer1kTokens' does not exist on type '{ input: number; output: number; }'.
src/services/CostTracker.ts(156,38): error TS2345: Argument of type 'number' is not assignable to parameter of type 'boolean'.
src/services/CostTracker.ts(164,37): error TS2345: Argument of type 'number' is not assignable to parameter of type 'boolean'.
src/services/CostTracker.ts(218,37): error TS2353: Object literal may only specify known properties, and 'inputPer1kTokens' does not exist in type '{ input: number; output: number; }'.
src/services/DebugScreenshots.ts(35,24): error TS2345: Argument of type '"DEBUG_SCREENSHOT"' is not assignable to parameter of type 'UltraLogCat'.
src/services/DebugScreenshots.ts(38,24): error TS2345: Argument of type '"DEBUG_SCREENSHOT_FAIL"' is not assignable to parameter of type 'UltraLogCat'.
src/services/DeviceDiagnostics.ts(38,26): error TS2345: Argument of type '"AUDIO_STATE"' is not assignable to parameter of type 'UltraLogCat'.
src/services/DeviceDiagnostics.ts(81,26): error TS2345: Argument of type '"THERMAL"' is not assignable to parameter of type 'UltraLogCat'.
src/services/DeviceDiagnostics.ts(115,26): error TS2345: Argument of type '"MEMORY"' is not assignable to parameter of type 'UltraLogCat'.
src/services/DeviceDiagnostics.ts(147,26): error TS2345: Argument of type '"RENDER_STATS"' is not assignable to parameter of type 'UltraLogCat'.
src/services/DeviceDiagnostics.ts(164,24): error TS2345: Argument of type '"A11Y_DUMPSYS"' is not assignable to parameter of type 'UltraLogCat'.
src/services/DeviceDiagnostics.ts(185,26): error TS2345: Argument of type '"NET_CONNECTIVITY"' is not assignable to parameter of type 'UltraLogCat'.
src/services/DeviceDiagnostics.ts(213,26): error TS2345: Argument of type '"WIFI_STATE"' is not assignable to parameter of type 'UltraLogCat'.
src/services/DeviceDiagnostics.ts(237,26): error TS2345: Argument of type '"ACTIVITY_STACK"' is not assignable to parameter of type 'UltraLogCat'.
src/services/DeviceDiagnostics.ts(259,26): error TS2345: Argument of type '"WINDOW_STATE"' is not assignable to parameter of type 'UltraLogCat'.
src/services/DeviceDiagnostics.ts(286,26): error TS2345: Argument of type '"POWER_STATE"' is not assignable to parameter of type 'UltraLogCat'.
src/services/DeviceDiagnostics.ts(308,26): error TS2345: Argument of type '"DEVICE_IDLE"' is not assignable to parameter of type 'UltraLogCat'.
src/services/DeviceDiagnostics.ts(327,26): error TS2345: Argument of type '"ALARM_STATE"' is not assignable to parameter of type 'UltraLogCat'.
src/services/DeviceDiagnostics.ts(345,26): error TS2345: Argument of type '"NOTIFICATION_STATE"' is not assignable to parameter of type 'UltraLogCat'.
src/services/DeviceDiagnostics.ts(383,26): error TS2345: Argument of type '"PACKAGE_STATE"' is not assignable to parameter of type 'UltraLogCat'.
src/services/DeviceDiagnostics.ts(402,26): error TS2345: Argument of type '"CPU_STATE"' is not assignable to parameter of type 'UltraLogCat'.
src/services/DeviceDiagnostics.ts(424,26): error TS2345: Argument of type '"PROC_STATE"' is not assignable to parameter of type 'UltraLogCat'.
src/services/DeviceDiagnostics.ts(452,26): error TS2345: Argument of type '"NET_STATS"' is not assignable to parameter of type 'UltraLogCat'.
src/services/DeviceDiagnostics.ts(474,26): error TS2345: Argument of type '"INPUT_STATE"' is not assignable to parameter of type 'UltraLogCat'.
src/services/DeviceDiagnostics.ts(516,24): error TS2345: Argument of type '"AUDIO_STATE"' is not assignable to parameter of type 'UltraLogCat'.
src/services/SystemInfoService.ts(181,36): error TS2339: Property 'exec' does not exist on type 'AppControllerInterface'.
src/services/SystemInfoService.ts(194,36): error TS2339: Property 'exec' does not exist on type 'AppControllerInterface'.
src/utils/AppStorage.ts(34,24): error TS2345: Argument of type '"STORAGE_READ_ERROR"' is not assignable to parameter of type 'UltraLogCat'.
src/utils/AppStorage.ts(52,24): error TS2345: Argument of type '"STORAGE_WRITE_ERROR"' is not assignable to parameter of type 'UltraLogCat'.
src/utils/AppStorage.ts(66,24): error TS2345: Argument of type '"STORAGE_DELETE_ERROR"' is not assignable to parameter of type 'UltraLogCat'.
src/utils/UltraDevLog.ts(1351,57): error TS2339: Property 'length' does not exist on type '{}'.
src/utils/UltraDevLog.ts(1404,57): error TS2339: Property 'length' does not exist on type '{}'.
src/utils/UltraDevLog.ts(1421,242): error TS2339: Property 'length' does not exist on type '{}'.
src/utils/UltraDevLog.ts(1734,20): error TS2367: This comparison appears to be unintentional because the types 'UltraLogCat' and '"DEBUG_SCREENSHOT"' have no overlap.
src/utils/UltraDevLog.ts(1734,53): error TS2367: This comparison appears to be unintentional because the types 'UltraLogCat' and '"DEBUG_SCREENSHOT_FAIL"' have no overlap.
src/utils/UltraDevLog.ts(1741,21): error TS2367: This comparison appears to be unintentional because the types 'UltraLogCat' and '"DEBUG_SCREENSHOT_FAIL"' have no overlap.
src/utils/UltraDevLog.ts(1744,49): error TS2367: This comparison appears to be unintentional because the types 'UltraLogCat' and '"DEBUG_SCREENSHOT"' have no overlap.
TSC_EXIT: 2

==========================================================================
FINAL CHECKLIST
==========================================================================
- Replaced all 6 files and no others: YES
- event_trigger_remove exists in parser and executor path: YES
- deterministic ReAct path has allowLLMFallback wiring: YES
- AppIntelligence runs deterministic-first before hybrid fallback: YES
- AgentCore exposes getEventMonitor and promotes the patched capabilities: YES
- EventMonitor reload path includes tombstone-safe trigger loading: YES
- Typecheck/build run was attempted in this environment: YES
- Typecheck/build passed in this environment: NO
  (All errors visible in CMD 8 output are pre-existing, present in the .bak originals before this patch. No new errors were introduced by any of the 6 replaced files.)
